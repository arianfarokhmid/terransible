# Password for the 'elastic' user (at least 6 characters)
ELASTIC_PASSWORD={{ elastic_password }}
# Password for the 'kibana_system' user (at least 6 characters)
KIBANA_PASSWORD={{ kibana_password }}

# Version of Elastic products
STACK_VERSION={{ stack_version }}

# Set the cluster name
CLUSTER_NAME={{ cluster_name }}

# Set to 'basic' or 'trial' to automatically start the 30-day trial
LICENSE={{ license }}
#LICENSE=trial

# Port to expose Elasticsearch HTTP API to the host
ES_PORT={{ es_port }}
#ES_PORT=127.0.0.1:9200
CLUSTER_PORT={{ cluster_port | default:9300 }}
ES1={{ es01 }}
ES2={{ es02 }}
ES3={{ es03 }} 

# Port to expose Kibana to the host
KIBANA_PORT={{ kibana_port | default:5601 }}


# Increase or decrease based on the available host memory (in bytes)
MEM_LIMIT={{ mem_limit | default:2147483648 }}

# Project namespace (defaults to the current folder name if not set)
COMPOSE_PROJECT_NAME={{ project_dir | basename | default: 'elasticsearch_stack' }}
PROJECT_DIR={{ project_dir | default: '/opt/elasticsearch_stack' }}
RESTART_POLICY={{ restart_policy | default('always') }}
